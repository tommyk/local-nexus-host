version: '2'
services:
  nexus:
    image: sonatype/nexus3
    volumes:
      - /nexus-data:/nexus-data
  nginx:
    build:
      context: .
      dockerfile: ./nginx-dockerfile
    ports:
      - "443:443"
    links:
      - nexus